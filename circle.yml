
test:
  override:
    # maybe we will need tests in the future
    - echo 'ok!'

deployment:
  prod:
    branch: master
    commands:
      - bash scripts/deploy.sh

machine:
  node:
    version: 6.1.0
